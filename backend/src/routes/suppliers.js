const express = require('express'); const db = require('../db'); const { authenticate, authorize } = require('../auth');
const router = express.Router(); router.use(authenticate);
router.get('/', async (req, res, next) => { try { const [rows] = await db.execute('SELECT id, name, contact_person, phone, email, payment_terms FROM suppliers WHERE branch_id = ? AND is_active = TRUE ORDER BY name', [req.user.branch_id]); res.json({ success: true, data: rows }); } catch (e) { next(e); } });
router.post('/', authorize('owner', 'manager', 'admin'), async (req, res, next) => { try { const { name, contact_person: contact, phone, email, address, payment_terms: terms, notes } = req.body; if (!name?.trim()) return res.status(400).json({ success: false, message: 'Nama supplier wajib diisi' }); const [r] = await db.execute('INSERT INTO suppliers (branch_id, name, contact_person, phone, email, address, payment_terms, notes) VALUES (?, ?, ?, ?, ?, ?, ?, ?)', [req.user.branch_id, name.trim(), contact?.trim() || null, phone?.trim() || null, email?.trim() || null, address?.trim() || null, terms?.trim() || null, notes?.trim() || null]); res.status(201).json({ success: true, data: { id: r.insertId } }); } catch (e) { next(e); } });
module.exports = router;
